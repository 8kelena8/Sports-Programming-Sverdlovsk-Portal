export type DisciplineAPI = {
  id: number;
  name: string;
  history?: string;
  rules?: string;
};

export type Discipline = {
  id: number;
  name: string;
  history?: string;
  rules?: string;
};
